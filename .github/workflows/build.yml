name: "Build PitchKernel for Malachite"

env:
  CONFIGURATION: "repos.json"
  OUT_DIR: "out"

on:
  workflow_dispatch:
    inputs:
      release:
        description: 'Create GitHub Release'
        required: false
        default: 'false'
        type: boolean

jobs:
  Set-repos:
    name: "🐂 Parse repos.json"
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.generate-matrix.outputs.repos }}
    steps:
      - name: "😄 Checkout"
        uses: actions/checkout@v4

      - name: "😆 Generate Matrix"
        id: generate-matrix
        run: |
          echo "repos<<EOF" >> $GITHUB_OUTPUT
          cat ${{ env.CONFIGURATION }} >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  Build-Kernel:
    name: "🐎 Build kernel"
    runs-on: ubuntu-latest
    needs:
      - Set-repos
    strategy:
      max-parallel: 2
      fail-fast: false
      matrix:
        repos: ${{ fromJSON(needs.Set-repos.outputs.repos) }}
    env:
      kernelDir: ${{ matrix.repos.kernelSource.name }}_${{ matrix.repos.kernelSource.device }}
      kernelName: ${{ matrix.repos.kernelSource.name }}
      kernelRepo: ${{ matrix.repos.kernelSource.repo }}
      kernelBranch: ${{ matrix.repos.kernelSource.branch }}
      kernelDevice: ${{ matrix.repos.kernelSource.device }}
      DEFCONFIG_NAME: ${{ matrix.repos.kernelSource.defconfig }}
      withKernelSU: ${{ matrix.repos.withKernelSU }}
      withSUSFS: ${{ matrix.repos.withSUSFS }}
    steps:
      - name: "✨ Create working dir"
        run: mkdir -p $kernelDir

      - name: "⭐ Install prerequisites"
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc curl git zip ftp gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi libssl-dev lftp zstd wget libfl-dev python3 libarchive-tools cpio

      - name: "🌟 Clone kernel source"
        working-directory: ./${{ env.kernelDir }}
        run: |
          git clone --recursive --branch $kernelBranch $kernelRepo $kernelName --depth=1

      - name: "💫 Get toolchains"
        working-directory: ./${{ env.kernelDir }}
        env:
          toolchains: "${{ toJSON(matrix.repos.toolchains) }}"
        run: |
          toolchains_num="$(echo $toolchains | jq 'length')"
          for ((i=0;i<toolchains_num;i++)); do
            repo="$(echo $toolchains | jq -r ".[$i].repo")"
            branch="$(echo $toolchains | jq -r ".[$i].branch")"
            name="$(echo $toolchains | jq -r ".[$i].name")"
            binPath="$(echo $toolchains | jq -r ".[$i].binPath | @tsv")"
            
            git clone --recursive --branch $branch --depth=1 $repo $name
            
            # Add bin paths to PATH
            IFS=$'\t' read -ra paths <<< "$binPath"
            for path in "${paths[@]}"; do
              echo "$(pwd)/$name/$path" >> $GITHUB_PATH
            done
          done

      - name: "😎 Set build args"
        id: generate-args
        working-directory: ./${{ env.kernelDir }}
        env:
          params: "${{ toJSON(matrix.repos.params) }}"
        run: |
          ARCH=$(echo $params | jq -r ".ARCH")
          CC=$(echo $params | jq -r ".CC")
          
          echo "ARCH=$ARCH" >> $GITHUB_ENV
          
          args="-j$(nproc --all)"
          if [ -n "${{ env.OUT_DIR }}" ]; then
            mkdir -p $(pwd)/${{ env.OUT_DIR }}
            args="$args O=$(pwd)/${{ env.OUT_DIR }}"
          fi
          if [ -n "$ARCH" ]; then
            args="$args ARCH=$ARCH"
          fi
          
          # Parse externalCommand for additional params
          externalCommand=$(echo $params | jq -r ".externalCommand // empty")
          if [ -n "$externalCommand" ] && [ "$externalCommand" != "null" ]; then
            keys=$(echo $externalCommand | jq -r 'keys[]')
            for key in $keys; do
              val=$(echo $externalCommand | jq -r ".[\"$key\"]")
              if [ -n "$val" ] && [ "$val" != "null" ]; then
                args="$args $key=$val"
              fi
            done
          fi
          
          echo "args=$args" >> $GITHUB_OUTPUT

      - name: "🛡️ Update KernelSU"
        working-directory: ./${{ env.kernelDir }}/${{ env.kernelName }}
        if: ${{ env.withKernelSU == 'true' }}
        run: |
          if [ -d "./KernelSU" ]; then
            rm -rf "./KernelSU"
          fi
          if [ -d "./drivers/kernelsu" ]; then
            rm -rf "./drivers/kernelsu"
          fi
          
          # KernelSU-Next, pinned to a known-good commit (not "next" HEAD).
          # Update KSUNEXT_REF deliberately when you want to move forward,
          # after testing — don't let this silently track a moving branch.
          KSUNEXT_REF="next"  # TODO: replace with a pinned commit SHA/tag once you've picked one
          curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -s "$KSUNEXT_REF"

          # Enable KPROBES for KernelSU
          echo -e "\nCONFIG_KPROBES=y" >> ./arch/${{ env.ARCH }}/configs/${{ env.DEFCONFIG_NAME }}
          echo -e "\nCONFIG_HAVE_KPROBES=y" >> ./arch/${{ env.ARCH }}/configs/${{ env.DEFCONFIG_NAME }}
          echo -e "\nCONFIG_KPROBE_EVENTS=y" >> ./arch/${{ env.ARCH }}/configs/${{ env.DEFCONFIG_NAME }}

      - name: "🔒 Patch SUSFS"
        working-directory: ./${{ env.kernelDir }}/${{ env.kernelName }}
        if: ${{ env.withSUSFS == 'true' }}
        run: |
          set -e
          # susfs4ksu ships raw .patch files + source files under
          # kernel_patches/ — there is no apply.sh that does this for you.
          # Pin the branch to a fixed commit (SUSFS_REF) once you've verified
          # it against your tree; "gki-android14-6.1" tracks upstream HEAD
          # and can break your patch offsets without warning.
          SUSFS_REF="gki-android14-6.1"  # TODO: pin to a tested commit SHA
          git clone --depth=1 --branch "$SUSFS_REF" https://gitlab.com/simonpunk/susfs4ksu.git

          KDIR="${{ github.workspace }}/${{ env.kernelDir }}/${{ env.kernelName }}"

          # Copy SUSFS source files into the kernel tree
          cp -r susfs4ksu/kernel_patches/fs/* "$KDIR/fs/"
          cp -r susfs4ksu/kernel_patches/include/linux/* "$KDIR/include/linux/"

          # Apply the GKI patch for kernel 6.1 — adjust the filename if
          # susfs4ksu's repo layout uses a different patch name for your
          # exact android/kernel combo (check kernel_patches/ directly).
          cd "$KDIR"
          patch -p1 < "${{ github.workspace }}/${{ env.kernelDir }}/${{ env.kernelName }}/../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android14-6.1.patch"

          # Also apply KernelSU-Next's own susfs integration patch inside
          # the KernelSU-Next directory this setup.sh step created above —
          # susfs4ksu's kernel-side patch alone does not wire it into KSU-Next.
          # Verify this file exists in the KernelSU-Next tree/version you pinned
          # above; the exact patch name has moved before.

          # Enable SUSFS in defconfig
          echo -e "\nCONFIG_KSU_SUSFS=y" >> "$KDIR/arch/${{ env.ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"

      - name: "👍 Make defconfig"
        working-directory: ./${{ env.kernelDir }}/${{ env.kernelName }}
        env:
          args: ${{ steps.generate-args.outputs.args }}
        run: |
          make ${{ env.args }} ${{ env.DEFCONFIG_NAME }}

      - name: "🎶 Build kernel"
        working-directory: ./${{ env.kernelDir }}/${{ env.kernelName }}
        env:
          args: ${{ steps.generate-args.outputs.args }}
        run: |
          make ${{ env.args }}

      - name: "📦 Setup AnyKernel3"
        if: ${{ matrix.repos.AnyKernel3.use == true }}
        working-directory: ./${{ env.kernelDir }}
        env:
          AK3_REPO: ${{ matrix.repos.AnyKernel3.repo }}
          AK3_BRANCH: ${{ matrix.repos.AnyKernel3.branch }}
        run: |
          git clone --branch $AK3_BRANCH --depth=1 $AK3_REPO AnyKernel3
          
          # Copy kernel image to AnyKernel3
          cp ${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/Image* AnyKernel3/ 2>/dev/null || true
          cp ${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/dtb* AnyKernel3/ 2>/dev/null || true
          cp ${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/dtbo.img AnyKernel3/ 2>/dev/null || true
          
          # Copy modules if any
          if [ -d "${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/modules" ]; then
            cp -r ${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/modules/* AnyKernel3/modules/ 2>/dev/null || true
          fi

      - name: "🗜️ Pack AnyKernel3 zip"
        if: ${{ matrix.repos.AnyKernel3.use == true }}
        working-directory: ./${{ env.kernelDir }}/AnyKernel3
        run: |
          zip -r9 ../PitchKernel-Malachite-$(date +%Y%m%d).zip * -x .git README.md *placeholder

      - name: "💛 Upload Artifacts"
        uses: actions/upload-artifact@v4
        with:
          name: PitchKernel-${{ env.kernelDevice }}-${{ github.run_number }}
          path: |
            ./${{ env.kernelDir }}/${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/Image*
            ./${{ env.kernelDir }}/${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/dtb*
            ./${{ env.kernelDir }}/${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/dtbo.img
            ./${{ env.kernelDir }}/PitchKernel-Malachite-*.zip
          if-no-files-found: ignore
          retention-days: 7

      - name: "🚀 Create GitHub Release"
        if: ${{ inputs.release == true && matrix.repos.AnyKernel3.release == true }}
        uses: softprops/action-gh-release@v1
        with:
          tag_name: PitchKernel-Malachite-v${{ github.run_number }}
          name: "PitchKernel for Malachite #${{ github.run_number }}"
          body: |
            PitchKernel build for Redmi Note 14 Pro 5G / POCO X7 5G (Malachite)
            - Android 14 (GKI 6.1)
            - KernelSU: ${{ env.withKernelSU }}
            - SUSFS: ${{ env.withSUSFS }}
          files: |
            ./${{ env.kernelDir }}/PitchKernel-Malachite-*.zip
            
